let mongoose,express;_4de‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_4de‍.w("express",[["default",["express"],function(v){express=v}]]);


const app = express();

const port = 3000



mongoose.connect('mongodb://localhost:27017/CarDatabase', {
    "useNewUrlParser": true,
    "useUnifiedTopology": true
});

const db = mongoose.connection;
db.on('error', _4de‍.g.console.error.bind(_4de‍.g.console, 'connection error:'));
db.once('open', () => {
  console.log('Database connected')
});

const userSchema = new mongoose.Schema({
    name: String,
    dateofbirth: Date,
    address1: String,
    address2: String,
    phone:Number
    
});

const user = mongoose.model('user', carSchema);

//add make to database
app.get('/addname/:name', (req, res) => {
    
    const aname = new user({name: req.params.name});

    aname.save()
    .then((result) => res.send(`${req.params.name} was saved to the database`))
    .catch((err) =>
    _4de‍.g.console.err(err));

});





app.listen(port, () => console.log(`Example app listening on 
  : ${port}!`))




